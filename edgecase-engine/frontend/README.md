# EdgeCase Engine - Frontend

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Defaults expect the backend at `http://localhost:4000/api`. To change, set `VITE_API_BASE` in environment.
